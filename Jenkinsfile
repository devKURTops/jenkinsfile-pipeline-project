pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "Jenkins let's do that again"
                sh 'echo friday'
                echo 'not using shell in the Jenkinsfile'
            }
        }
    }
}